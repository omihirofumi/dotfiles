theme = "tokyo-night"
vcs = "jj"
prompt_save_view_preferences = true
# jj の pager として使うと stdin 入力になり、再オープンできない watch はエラーになるため無効化。
# 直接起動して監視したいときは `hunk --watch`
watch = false
wrap_lines = true
mode = "unified"
